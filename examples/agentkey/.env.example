# AgentKey Environment Configuration

# StrongDM Identity Provider
STRONGDM_ISSUER=https://id.strongdm.ai
STRONGDM_AUDIENCE=agentkey

# OAuth2 Client Credentials (required for web auth flow)
STRONGDM_CLIENT_ID=cli_your_client_id
STRONGDM_CLIENT_SECRET=sec_your_client_secret
STRONGDM_CALLBACK_URL=http://localhost:9873/auth/callback

# Token Introspection (optional fallback)
STRONGDM_INTROSPECTION_ENABLED=false

# Database (leave empty for SQLite in development)
# DATABASE_URL=postgres://user:password@localhost:5432/agentkey

# Server Configuration
# PORT=9873
